apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: maas

resources:
  - ../../base
  - llamastack-config.yaml
  - openai-api-key-secret.yaml

patches:
  - path: patches/deployment-patch.yaml
    target:
      kind: LLMInferenceService
      name: llamastack

namePrefix: openai-

labels:
  - pairs:
      app.kubernetes.io/instance: openai-llamastack
      provider: openai