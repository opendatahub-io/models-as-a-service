# Keycloak PoC Custom Image
# Based on official Keycloak image with PoC-specific configuration
FROM quay.io/keycloak/keycloak:26.5.1

# Set working directory
WORKDIR /opt/keycloak

# The base image already has Keycloak installed
# We can add any custom configuration here if needed
# For now, we're just using the base image with our custom deployment config

# Expose Keycloak HTTP port
EXPOSE 8080

# The base image already has the entrypoint configured
# We'll use start-dev mode in the deployment
