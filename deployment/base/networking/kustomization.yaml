apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: maas-api-all-infra

resources:
- odh
- maas
# ClusterIssuer for cert-manager (model TLS certificates)
- cluster-issuer.yaml