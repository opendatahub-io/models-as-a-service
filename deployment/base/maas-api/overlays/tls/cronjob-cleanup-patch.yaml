# CronJob base uses http://maas-api:8080; TLS overlay exposes HTTPS on 8443 only.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: maas-api-key-cleanup
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: cleanup
            command:
            - /bin/sh
            - -c
            - |
              TOKEN="$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)" && \
              curl -sf -k -X POST -H "Authorization: Bearer ${TOKEN}" https://maas-api:8443/internal/v1/api-keys/cleanup
