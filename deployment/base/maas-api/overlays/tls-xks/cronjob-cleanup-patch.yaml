# CronJob base uses http://maas-api:8080; TLS overlay serves HTTPS on 8443 only.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: maas-api-key-cleanup
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: cleanup
            command:
            - /bin/sh
            - -c
            - |
              curl -sf --connect-timeout 5 --max-time 30 --cacert /etc/maas-api-ca/tls.crt -X POST https://maas-api:8443/internal/v1/api-keys/cleanup
            volumeMounts:
            - name: maas-api-ca
              mountPath: /etc/maas-api-ca
              readOnly: true
          volumes:
          - name: maas-api-ca
            secret:
              secretName: maas-api-serving-cert
              items:
              - key: tls.crt
                path: tls.crt
