apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../after
  - ../pre-processing
  # FIXME: EnvoyFilter disabled - payload-processing controller crashes looking for
  # CRDs in wrong API group (inference.opendatahub.io instead of maas.opendatahub.io)
  # - envoy-filter.yaml
  - plugins-configmap.yaml

images:
  - name: payload-processing
    newName: quay.io/opendatahub/odh-ai-gateway-payload-processing
    newTag: odh-stable
