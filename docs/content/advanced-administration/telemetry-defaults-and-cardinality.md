# Telemetry Defaults and Cardinality Guidance

This document provides recommended defaults for MaaS telemetry dimensions, explains cardinality risks, and gives guidance for safely scaling observability.

!!! warning "Estimates, Not Load-Tested"
    The projections in this document are analytical estimates based on the Prometheus time-series model, not validated with load testing. Real-world cardinality depends on usage patterns, model adoption breadth, and label value counts that vary by deployment. These estimates should be validated with customer scale data before being used for capacity planning.

## How MaaS Telemetry Works

Per-user metrics are generated by Limitador (Kuadrant's rate-limiting engine) at the Istio gateway, not by maas-api. The [TelemetryPolicy](../../../deployment/base/observability/gateway-telemetry-policy.yaml) defines which labels are added to Limitador counters:

| Label | Source | Default | Cardinality |
|-------|--------|---------|-------------|
| `user` | `auth.identity.userid` | Enabled | **Unbounded** — grows with user base |
| `model` | `responseBodyJSON("/model")` | Enabled | **Unbounded** — grows with deployed models (see [Model Cardinality](#model-cardinality)) |
| `subscription` | `auth.identity.selected_subscription` | Enabled | **Unbounded** — grows with MaaSSubscription CRs |
| `organization_id` | `auth.identity.subscription_info.organizationId` | Enabled | **Unbounded** — grows with tenant count |
| `cost_center` | `auth.identity.subscription_info.costCenter` | Enabled | **Unbounded** — grows with org structure |

!!! danger "All labels are effectively unbounded"
    Unless MaaS enforces hard limits on unique values per label, every dimension can grow without constraint. While `model`, `subscription`, `organization_id`, and `cost_center` grow more slowly than `user`, they are not bounded by the platform. A deployment with 200 models (per RHAISTRAT-730 medium-large cluster sizing), 50 subscriptions, 20 orgs, and 50 cost centers produces a worst-case cross-product of **~1.6M series across 3 metrics** before the `user` dimension is even considered (see [Cardinality Analysis](#cardinality-analysis)).

## Metrics Affected

| Metric | Type | Labels | Purpose |
|--------|------|--------|---------|
| `authorized_hits` | Counter | All above + `limitador_namespace` | Token consumption (from `usage.total_tokens` in response body) |
| `authorized_calls` | Counter | All except `model` | Requests allowed through rate limiting |
| `limited_calls` | Counter | All except `model` | Requests denied by rate limits |

Metrics that intentionally avoid high-cardinality labels:

- `istio_request_duration_milliseconds_bucket` — uses `subscription` only (histograms have high base cardinality from bucket boundaries)
- `vllm:*` — uses `model_name` only (vLLM has no visibility into user identity)

### Model Cardinality

The `model` label is not bounded by MaaS. Real deployments can have significantly more models than a typical dev cluster:

| Deployment Size | Typical Model Count | Source |
|----------------|-------------------|--------|
| Dev/test | 5-10 | Small set for development |
| Production (small) | 10-50 | Focused enterprise use case |
| Production (medium-large) | ~200 | RHAISTRAT-730 sizing guidance |
| Aggregator platforms | 500+ | e.g., OpenRouter serves 653 models |

A single API call to any model creates time series in the retention period. Users who experiment across many models amplify the effective cardinality multiplier.

## Cardinality Analysis

### Worst-Case Cross-Product

The theoretical maximum series count for a single metric is the full cross-product of all label values:

```
worst_case = users × models × subscriptions × orgs × cost_centers × limitador_namespaces
```

Using mid-range values from the current TelemetryPolicy labels (27 models, 27 subscriptions, 10 orgs, 25 cost centers, 3 limitador namespaces):

```
27 × 27 × 10 × 25 × 3 ≈ 546,750 series (single metric, before user dimension)
```

With all three metrics (`authorized_hits`, `authorized_calls`, `limited_calls`) this approaches **1.6M series** — already exceeding default OpenShift User Workload Monitoring capacity without any per-user dimension.

This already illustrates the cardinality risk even without per-user breakdown.

### Effective Multiplier Per User

In practice, not every user hits every model/subscription/org combination. An effective multiplier of unique label combinations per active user depends on usage patterns:

| User Behavior | Effective Multiplier | Example |
|--------------|---------------------|---------|
| Single model, single subscription | ~1-2 | Dedicated inference endpoint |
| Experimenting across models | ~10-15 | Developer trying gemini + claude variants (~15 models) |
| Cross-subscription access | ~20-30 | User with access to multiple tiers/subscriptions |

!!! note "Assumption Needs Validation"
    The effective multiplier per user is a critical assumption that has not been validated with real customer data. The spike originally assumed ~5 combinations per user, which is likely optimistic for deployments with many models. This assumption should be vetted with production telemetry from willing customers before engineering around it.

### Projections (Conservative: 15 combinations/user)

Using a more conservative multiplier of 15 label combinations per active user:

| Active Users | Estimated Series | Impact |
|-------------|-----------------|--------|
| 100 | ~4,500 | Negligible |
| 1,000 | ~45,000 | Comfortable |
| 10,000 | ~450,000 | Approaching default Prometheus limits |
| 50,000 | ~2,250,000 | Exceeds default OpenShift User Workload Monitoring |
| 100,000 | ~4,500,000 | Requires dedicated or remote storage |

!!! note "Active vs Total Users"
    Only users who made at least one request during the retention period create series. Inactive users do not contribute to cardinality.

## Impact on Limitador

Cardinality does not only affect Prometheus. Each unique label combination creates a counter inside Limitador itself:

| Impact Area | Description |
|------------|-------------|
| **Memory** | Limitador holds all active counters in memory (or Redis). High cardinality increases memory pressure on the Limitador pod. |
| **`/metrics` endpoint** | Prometheus scrapes Limitador's `/metrics` endpoint every 30s. Each series adds ~100 bytes to the response. At 500k series, scrape responses reach ~50 MB, potentially exceeding the 30s scrape interval and causing missed scrapes. |
| **Counter lookup** | Each incoming request requires a counter lookup by label combination. Larger counter sets may increase lookup latency, though this is typically sub-millisecond with hash-based storage. |
| **Redis (if used)** | With Redis-backed persistence, each counter is a Redis key. High cardinality increases Redis memory usage and network traffic for counter sync. |

!!! warning "Limitador impact is not well-characterized"
    The effect of high label cardinality on Limitador performance (memory, lookup latency, scrape duration) has not been load-tested. This is a gap that should be addressed before recommending per-user labels at scale.

## Dangerous Combinations

!!! warning "High-Risk Configurations"
    Enabling all dimensions at scale can overwhelm both Prometheus and Limitador:

    - **All labels enabled + many models**: With 200 models and 50 subscriptions, the non-user dimensions alone produce millions of potential series.
    - **Per-user histograms**: Never add `user` to Istio latency histograms. Each histogram has 23 buckets by default — with 10k users this creates 230k series from one metric alone.
    - **Short scrape intervals**: Reducing scrape interval below 30s doubles ingestion load without proportional observability benefit. At high cardinality, scrape duration may already exceed the interval.

## Realistic Mitigation Options

### Option 1: Drop High-Cardinality Labels by Default (Recommended)

Disable per-user and other high-cardinality labels by default. Operators opt in to specific labels based on their scale and monitoring capacity.

This aligns with `captureUser: false` as the default. The TelemetryPolicy would ship with only low-risk labels enabled:

```yaml
spec:
  metrics:
    default:
      labels:
        model: responseBodyJSON("/model")
        # user, subscription, organization_id, cost_center disabled by default
        # Enable selectively based on deployment scale and monitoring capacity
```

Operators who need per-user billing data enable the `user` label explicitly, accepting the cardinality cost.

### Option 2: Move High-Cardinality Data to Logs

Instead of encoding `user`, `organization_id`, and `cost_center` as metric labels (which create time series), emit them as structured log entries. Logs can be aggregated and queried via log analytics (e.g., OpenShift Logging, Loki) without creating Prometheus cardinality pressure.

Metrics remain for aggregated operational visibility (per-model, per-tier). Logs handle per-user attribution and billing queries.

### Option 3: Enforce Hard Limits on Label Values

Add controller-side validation that caps the number of unique values for each label dimension. For example, reject TelemetryPolicy configurations that would produce more than a configurable cardinality budget. This makes the worst-case series count predictable.

### Recording Rules (Query Optimization Only)

Pre-aggregate metrics for dashboards so they don't scan all series on every panel load:

```yaml
groups:
- name: maas-aggregated
  interval: 1m
  rules:
  - record: maas:authorized_hits:rate5m_by_model
    expr: sum by (model) (rate(authorized_hits[5m]))
  - record: maas:authorized_calls:rate5m_by_subscription
    expr: sum by (subscription) (rate(authorized_calls[5m]))
```

!!! note "Recording rules are not a cardinality fix"
    Recording rules improve query performance but do not reduce storage, ingestion, or Limitador memory pressure. They are a useful complement to other mitigations, not a standalone solution.

### Cardinality Alert

Add this alert to detect cardinality growth before it causes problems:

```yaml
- alert: MaaSHighMetricCardinality
  expr: count({__name__=~"authorized_hits|authorized_calls|limited_calls"}) > 100000
  for: 10m
  labels:
    severity: warning
  annotations:
    summary: "MaaS Limitador metrics exceed 100k active series"
    description: "Review telemetry label cardinality. See docs/advanced-administration/telemetry-defaults-and-cardinality.md"
```

## Open Questions

The following questions should be answered with real customer data before finalizing telemetry defaults:

1. **What is the realistic effective multiplier per user?** How many unique model/subscription combinations does a typical user touch during a retention period?
2. **What is the Limitador performance impact?** At what counter count does Limitador's `/metrics` scrape exceed the scrape interval? Does counter lookup latency degrade?
3. **What model counts are realistic?** Do production customers deploy 10 models or 200? This is the biggest variable after `user`.
4. **Which labels are needed for billing vs operational monitoring?** Can we separate the billing pipeline (logs or dedicated store) from operational metrics (Prometheus)?

## Prometheus Capacity Reference

| Configuration | Active Series Limit |
|--------------|-------------------|
| OpenShift User Workload Monitoring (default) | ~1M |
| OpenShift User Workload Monitoring (tuned, 16-32GB) | ~5M |
| Standalone Prometheus (64GB, NVMe) | ~10M |
| Mimir / Thanos / VictoriaMetrics | 100M+ |

## See Also

- [Observability Guide](observability.md) — full metrics reference, dashboards, and installation
- [Subscription and Policy Cardinality](subscription-cardinality.md) — guidance on `groups` vs `users` in MaaSAuthPolicy/MaaSSubscription
