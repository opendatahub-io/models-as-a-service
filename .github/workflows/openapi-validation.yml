name: OpenAPI Validation

on:
  pull_request:
    paths:
      - 'maas-api/openapi3.yaml'
      - '.spectral.yml'
      - '.github/workflows/openapi-validation.yml'
  push:
    branches: [main, master]
    paths:
      - 'maas-api/openapi3.yaml'

permissions:
  contents: read

jobs:
  validate-spec:
    name: Validate OpenAPI Specification
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Spectral
        run: npm install -g @stoplight/spectral-cli

      - name: Validate OpenAPI Spec
        run: |
          echo "🔍 Validating OpenAPI specification..."
          spectral lint maas-api/openapi3.yaml --format stylish --verbose

      - name: Generate Validation Report
        if: always()
        run: |
          echo "📊 Generating detailed validation report..."
          spectral lint maas-api/openapi3.yaml --format json > openapi-validation-report.json || true

          # Show summary
          if [ -f openapi-validation-report.json ]; then
            echo "## OpenAPI Validation Summary" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            ERROR_COUNT=$(jq '[.[] | select(.severity == 0)] | length' openapi-validation-report.json)
            WARN_COUNT=$(jq '[.[] | select(.severity == 1)] | length' openapi-validation-report.json)
            INFO_COUNT=$(jq '[.[] | select(.severity == 2)] | length' openapi-validation-report.json)
            HINT_COUNT=$(jq '[.[] | select(.severity == 3)] | length' openapi-validation-report.json)

            echo "- ❌ Errors: $ERROR_COUNT" >> $GITHUB_STEP_SUMMARY
            echo "- ⚠️  Warnings: $WARN_COUNT" >> $GITHUB_STEP_SUMMARY
            echo "- ℹ️  Info: $INFO_COUNT" >> $GITHUB_STEP_SUMMARY
            echo "- 💡 Hints: $HINT_COUNT" >> $GITHUB_STEP_SUMMARY

            if [ "$ERROR_COUNT" -eq 0 ]; then
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "✅ **No errors found!** Spec is valid." >> $GITHUB_STEP_SUMMARY
            fi
          fi

      - name: Upload Validation Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openapi-validation-report
          path: openapi-validation-report.json
          retention-days: 30

  breaking-changes:
    name: Detect Breaking Changes
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout PR
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Need full history for comparison

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install oasdiff
        run: |
          curl -fsSL https://raw.githubusercontent.com/oasdiff/oasdiff/main/install.sh | sh
          oasdiff --version

      - name: Check for Breaking Changes
        id: breaking_changes
        continue-on-error: true
        run: |
          echo "🔍 Checking for API breaking changes..."

          # Get base branch spec
          git fetch origin ${{ github.base_ref }}
          git show origin/${{ github.base_ref }}:maas-api/openapi3.yaml > base-spec.yaml

          # Run breaking change detection
          oasdiff breaking base-spec.yaml maas-api/openapi3.yaml > breaking-changes.txt || true

          if [ -s breaking-changes.txt ]; then
            echo "has_breaking_changes=true" >> $GITHUB_OUTPUT
            echo "## ⚠️  Breaking Changes Detected" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            cat breaking-changes.txt >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          else
            echo "has_breaking_changes=false" >> $GITHUB_OUTPUT
            echo "## ✅ No Breaking Changes" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "This PR does not introduce any breaking API changes." >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload Breaking Changes Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: breaking-changes-report
          path: breaking-changes.txt
          retention-days: 30

      - name: Fail on Breaking Changes
        if: steps.breaking_changes.outputs.has_breaking_changes == 'true'
        run: |
          echo "❌ Breaking changes detected. If intentional, document in PR and get approval."
          echo "   Consider:"
          echo "   - Is this a major version bump?"
          echo "   - Are clients given migration time?"
          echo "   - Is there a deprecation notice?"
          exit 1

  changelog-check:
    name: Check API Changelog
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Need full history for git diff

      - name: Check if API changed
        id: api_changed
        run: |
          if git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -q "maas-api/openapi3.yaml"; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Verify Changelog Entry
        if: steps.api_changed.outputs.changed == 'true'
        run: |
          echo "📝 Checking for API changelog entry..."

          # Check if there's an API changelog file
          if [ ! -f docs/content/release-notes/api-changelog.md ]; then
            echo "⚠️  No API changelog found at docs/content/release-notes/api-changelog.md" >> $GITHUB_STEP_SUMMARY
            echo "   Consider creating one to track API changes over time." >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

          # Check if changelog was updated in this PR
          if git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -q "docs/content/release-notes/api-changelog.md"; then
            echo "✅ API changelog updated" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️  API spec changed but changelog not updated" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Consider adding an entry to docs/content/release-notes/api-changelog.md" >> $GITHUB_STEP_SUMMARY
          fi
